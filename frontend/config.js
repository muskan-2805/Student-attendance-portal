// Central place for the backend API base URL.
// Change this ONE line when moving from local dev to staging/production —
// every page pulls from here instead of hardcoding http://localhost:5000.
const API_BASE_URL = "http://localhost:5000";
